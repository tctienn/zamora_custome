extend type Query {
    docInProcessedStatistic(year: Int!, userDeptRole: UserDeptRole!): DocStatistic
    docInCommentedStatistic(year: Int!, userDeptRole: UserDeptRole!): DocStatistic
    docOutProcessedStatistic(year: Int!, userDeptRole: UserDeptRole!): DocStatistic
    docOutCommentedStatistic(year: Int!, userDeptRole: UserDeptRole!): DocStatistic
}

type DocStatistic {
    jan: Int
    feb: Int
    mar: Int
    apr: Int
    may: Int
    jun: Int
    jul: Int
    aug: Int
    sep: Int
    oct: Int
    nov: Int
    dec: Int
}
